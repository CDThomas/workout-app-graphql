# import User, Routine, Exercise from "./generated/prisma.graphql"

type Query {
  exercises: [Exercise!]!
  routine(id: ID!): Routine
  routines: [Routine!]!
  routinesByUser(userId: ID!): [Routine!]!
}

type Mutation {
  createUser(name: String!): User
  createExercise(name: String!): Exercise
}
