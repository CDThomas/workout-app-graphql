# import User, Routine from "./generated/prisma.graphql"

type Query {
  routine(id: ID!): Routine
  routines: [Routine!]!
  routinesByUser(userId: ID!): [Routine!]!
}

type Mutation {
  createUser(name: String!): User
}
